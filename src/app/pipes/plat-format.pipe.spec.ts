import { PlatFormatPipe } from './plat-format.pipe';

describe('PlatFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
