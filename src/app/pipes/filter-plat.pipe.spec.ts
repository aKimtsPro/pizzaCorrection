import { FilterPlatPipe } from './filter-plat.pipe';

describe('FilterPlatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlatPipe();
    expect(pipe).toBeTruthy();
  });
});
